# Patientor backend (exercise 9.8)

Simple Express + TypeScript project for the Fullstackopen exercise.

Run locally in development mode:

```bash
cd patientor-backend
npm install
npm run dev
```

Build and run compiled code:

```bash
npm run build
npm start
```

Endpoints:

- GET `/api/ping` -> { message: 'pong' }
